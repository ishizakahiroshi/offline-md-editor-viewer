================================================================================
DESKTOP DEPENDENCIES (Tauri / Rust crates statically linked into the Windows exe)
================================================================================

This list is generated by `cargo about` from `apps/desktop/src-tauri/Cargo.lock`.
Crates are grouped by their SPDX license identifier. The full license text for
each group is included below.

Overview:
{{#each overview}}
  - {{{name}}} ({{count}} packages)
{{/each}}

{{#each licenses}}
================================================================================
{{{name}}} ({{{id}}})
================================================================================

Used by:
{{#each used_by}}
  - {{{crate.name}}} {{{crate.version}}}{{#if crate.repository}} ({{{crate.repository}}}){{/if}}
{{/each}}

License text:
--------------------------------------------------------------------------------
{{{text}}}
--------------------------------------------------------------------------------

{{/each}}
