{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allows the main window to use Tauri core IPC. File and folder operations are exposed through app-owned invoke commands instead of fs/dialog plugin permissions.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default"
  ]
}
